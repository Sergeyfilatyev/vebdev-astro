# 🔧 Исправление ошибки базы данных

## Проблема
Ошибка при создании таблицы `pages_blocks_portfolio`:
```
"button_text" varchar DEFAULT [object Object]
```

Это происходит потому, что Payload пытается использовать объект JavaScript как DEFAULT значение в PostgreSQL, что невозможно.

## Решение

### Вариант 1: Очистить кэш и перезапустить (рекомендуется)

```bash
cd cms
rm -rf .next
pnpm dev
```

### Вариант 2: Удалить проблемные таблицы из базы данных

Если первый вариант не помог, нужно удалить старые таблицы:

```sql
-- Подключитесь к вашей базе данных PostgreSQL
-- Затем выполните:

DROP TABLE IF EXISTS pages_blocks_portfolio CASCADE;
DROP TABLE IF EXISTS pages_blocks_portfolio_locales CASCADE;

-- Также можно удалить все связанные таблицы блоков, если нужно:
DROP TABLE IF EXISTS pages_blocks_steps_block CASCADE;
DROP TABLE IF EXISTS pages_blocks_faq_block CASCADE;
DROP TABLE IF EXISTS pages_blocks_stats_stats CASCADE;
DROP TABLE IF EXISTS pages_blocks_stats CASCADE;
DROP TABLE IF EXISTS pages_blocks_contact CASCADE;
```

После этого перезапустите сервер:
```bash
cd cms
pnpm dev
```

Payload автоматически пересоздаст таблицы с правильной схемой.

### Вариант 3: Полная очистка схемы (если ничего не помогает)

⚠️ **ВНИМАНИЕ: Это удалит ВСЕ данные из базы данных!**

```sql
-- Удалить все таблицы Payload
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO postgres;
GRANT ALL ON SCHEMA public TO public;
```

Затем перезапустите сервер - Payload создаст все таблицы заново.

## Проверка

После исправления проверьте:
1. Сервер запускается без ошибок
2. Админка открывается: http://localhost:3000/admin
3. Можно создать новую страницу с блоком Portfolio

## Причина проблемы

Проблема была в том, что в коде использовался `defaultValue` с объектом для локализованных полей:
```typescript
defaultValue: { en: '...', ru: '...', uk: '...' }
```

PostgreSQL не может использовать объекты JavaScript как DEFAULT значения. Для локализованных полей в Payload CMS нельзя использовать объекты в `defaultValue` - значения хранятся в отдельных таблицах локалей.

Все такие `defaultValue` уже удалены из кода, но если в базе данных уже была создана таблица со старой схемой, нужно ее пересоздать.
