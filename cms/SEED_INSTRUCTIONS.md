# 🌱 Инструкция по запуску Seed скрипта

## Подготовка

1. Убедитесь, что база данных настроена и доступна
2. Установите зависимости:
   ```bash
   cd cms
   pnpm install
   ```

3. Убедитесь, что папка `veb-dev-studio` находится на том же уровне, что и `vebdev-astro`:
   ```
   /Users/Sergey/FSV/apps/
   ├── vebdev-astro/
   └── veb-dev-studio/
   ```

## Запуск

```bash
cd cms
pnpm seed
```

## Что будет загружено

✅ **16 проектов** из `veb-dev-studio/src/lib/db/projects.json`
✅ **4 статьи блога** из `veb-dev-studio/src/lib/db/blog.json`
✅ **6 услуг** из `veb-dev-studio/src/lib/db/services.json`
✅ **8 FAQ** из `veb-dev-studio/src/lib/db/faq.json`
✅ **10 этапов работы** из `veb-dev-studio/src/lib/db/steps.json`
✅ **SiteSettings** из `veb-dev-studio/messages/*.json` и `services_adv.json`

## Примечания

- Скрипт **не создает дубликаты** - если запись с таким `slug` уже существует, она пропускается
- **Изображения проектов** нужно будет загрузить вручную в коллекцию Media
- Все данные **локализованы** (uk, ru, en)

## Если возникли ошибки

1. Проверьте, что путь к `veb-dev-studio` правильный
2. Убедитесь, что база данных доступна
3. Проверьте, что все коллекции созданы в Payload CMS
4. Убедитесь, что `.env` файл настроен правильно
