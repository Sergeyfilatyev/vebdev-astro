# 🧪 Тестирование CMS

## Быстрая проверка структуры

Все файлы созданы и структура соответствует паттерну из `transinpack`:

### ✅ Коллекции (9 файлов)
- `Users.ts` ✅
- `Media.ts` ✅
- `Pages.ts` ✅ (использует blocks из index.ts)
- `Projects.ts` ✅
- `Articles.ts` ✅
- `Services.ts` ✅
- `FAQ.ts` ✅
- `Steps.ts` ✅
- `ContactSubmissions.ts` ✅

### ✅ Блоки (11 файлов)
- `Hero.ts` ✅ (с вариантами)
- `Content.ts` ✅ (улучшен)
- `MediaBlock.ts` ✅
- `Advantages.ts` ✅
- `Services.ts` ✅
- `Portfolio.ts` ✅
- `Steps.ts` ✅
- `FAQ.ts` ✅
- `Stats.ts` ✅
- `Contact.ts` ✅
- `index.ts` ✅ (экспорт всех блоков)

### ✅ Глобальные настройки
- `SiteSettings.ts` ✅ (10 вкладок)

### ✅ Утилиты
- `collectionHooks.ts` ✅

## Запуск для тестирования

```bash
cd cms
pnpm dev
```

Откройте http://localhost:3000/admin

## Что проверить

1. **Админка открывается** - нет ошибок компиляции
2. **Все коллекции видны** в левом меню
3. **SiteSettings доступен** в меню
4. **В Pages можно добавить блоки** - все 10 блоков доступны
5. **Локализация работает** - переключение языков (uk, ru, en)

## Если есть ошибки

1. Проверьте `.env` файл:
   - `DATABASE_URI` - подключение к PostgreSQL
   - `PAYLOAD_SECRET` - секретный ключ
   - S3 переменные (если используются)

2. Проверьте логи в консоли

3. Запустите генерацию типов:
   ```bash
   pnpm generate:types
   ```

## Статус

✅ Все файлы созданы
✅ Структура соответствует роадмапу
✅ Линтер не находит ошибок
⏳ Готово к запуску и тестированию
